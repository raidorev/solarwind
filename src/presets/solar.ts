import { type SolarwindConfig } from 'solarwind'
import { solar } from 'solarwind/presets/themes/solar'

export const preset: SolarwindConfig = {
  orientation: 'ltr',
  theme: 'solar',
  themes: { solar },
}
